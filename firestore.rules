rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow public read access (so anyone can see the cars)
      allow read: if true;
      
      // Allow write access only to authenticated users (admins)
      allow write: if request.auth != null;
    }
  }
}
